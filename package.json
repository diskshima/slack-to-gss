{
  "name": "gas-slack-ss-node",
  "version": "0.0.1",
  "description": "Google App Script to copy Slack messages to a Google Spreadsheet",
  "main": "app.js",
  "scripts": {
    "build": "babel src -d dist",
    "copy": "babel src -d dist && cat dist/app.js | pbcopy"
  },
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-polyfill": "^6.23.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-react": "^6.24.1",
    "flow-bin": "^0.51.1"
  }
}
